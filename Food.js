function Food (){
	
	//this.gridCount = 899;

	this.createFood =  function (a) {
		// take in snake array
		// while loop of random to find a spot that isn't where the snake is. 
		var r = floor(random(0, 899));
		return r;
		//this.exists = true;
	}
}